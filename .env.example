# ============================================
# Le Trousseau - Configuration Environnement
# ============================================

# --- API BACKEND URL ---
# URL de l'API backend pour les soumissions de formulaires
# Développement: http://localhost:3001/api
# Production: https://api.asso-letrousseau.com/api
VITE_API_URL=http://localhost:3001/api

# --- MODE MOCK (Développement) ---
# true = utilise les mocks (pas de stockage réel ni email)
# false = utilise les providers réels configurés ci-dessous
VITE_USE_MOCK=true

# --- STORAGE PROVIDER ---
# Options: mock | hostinger_mysql | supabase
# 'mock' = stockage volatile en mémoire (perdu au refresh)
# 'hostinger_mysql' = base MySQL sur Hostinger (à implémenter)
# 'supabase' = Supabase Database (à implémenter)
VITE_STORAGE_PROVIDER=mock

# --- MAILER PROVIDER ---
# Options: mock | smtp | sendgrid
# 'mock' = simule envoi email en console
# 'smtp' = SMTP Hostinger ou autre
# 'sendgrid' = SendGrid API
VITE_MAILER_PROVIDER=mock

# --- NOTIFICATION EMAIL ---
# Adresse email qui recevra les notifications de soumission
VITE_NOTIFICATION_EMAIL=admin@letrousseau.com

# --- ADMIN EXPORT ---
# Token pour sécuriser l'endpoint d'export CSV/JSON
# Générer avec: openssl rand -hex 32
VITE_ADMIN_EXPORT_TOKEN=your-secret-token-here

# --- RGPD / DATA RETENTION ---
# Nombre de jours avant purge automatique (si implémenté)
# Ex: 365 = 1 an, 730 = 2 ans
VITE_DATA_RETENTION_DAYS=365

# ============================================
# CONFIGURATION HOSTINGER (à activer lors de la migration)
# ============================================

# --- MySQL Database ---
# DB_HOST=mysql.hostinger.com
# DB_NAME=u123456789_trousseau
# DB_USER=u123456789_admin
# DB_PASS=your-secure-password

# --- SMTP Email ---
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=587
# SMTP_USER=noreply@letrousseau.com
# SMTP_PASS=your-email-password
# SMTP_SECURE=false

# ============================================
# CONFIGURATION ALTERNATIVE (SendGrid, autres)
# ============================================

# --- SendGrid API ---
# SENDGRID_API_KEY=SG.your-api-key

# --- Google Sheets (optionnel) ---
# GOOGLE_SHEETS_ID=your-sheet-id
# GOOGLE_SHEETS_CREDENTIALS={"type":"service_account",...}

# ============================================
# CLOUDFLARE TURNSTILE (CAPTCHA)
# ============================================
VITE_TURNSTILE_SITE_KEY=your-turnstile-site-key

# ============================================
# NOTES IMPORTANTES
# ============================================
#
# 1. Les variables VITE_* sont exposées côté client
#    Ne jamais y mettre de secrets sensibles (passwords, API keys privées)
#
# 2. Pour la migration Hostinger:
#    - Décommenter les variables DB_* et SMTP_*
#    - Mettre VITE_USE_MOCK=false
#    - Mettre VITE_STORAGE_PROVIDER=hostinger_mysql
#    - Mettre VITE_MAILER_PROVIDER=smtp
#
# 3. Le fichier .env ne doit JAMAIS être commité dans Git
#    Il est déjà dans .gitignore
#
# 4. Pour production, utiliser les variables d'environnement
#    du serveur ou du service d'hébergement
#
